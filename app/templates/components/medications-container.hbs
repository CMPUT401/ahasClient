<div id="medications">
    {{#each medicationList as |item index|}}
      {{medication-input index=index patientID=patientID id=item.id reminder=item.reminder name=item.name update=(action 'updateMed') delete=(action 'deleteMed') }}
    {{/each}}
    <button {{action 'addMedication'}}>Add</button>
</div>
{{yield results}}