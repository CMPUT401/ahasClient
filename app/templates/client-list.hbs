<div class = "container">
	<!--TODO: add header with tabs -->
	<div class = "nameList" style="height: 200px; width: 300px">
		{{#client-list-filter filter=(action 'filterClientName')}}
			{{#ember-scrollable autoHide=false}}
				{{#each model as |client|}}
					<!-- TODO: onclick, GET client info and display it -->
					<div>
						<p>{{client.name}}</p>
					</div>
				{{/each}}
			{{/ember-scrollable}}
		{{/client-list-filter}}
	</div>
</div>
