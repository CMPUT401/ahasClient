
<div class = "container">
	{{#if session.isAuthenticated}}
	<nav class = "navbar navbar-inverse">
		<li class = "nav navbar-nav">
			{{#nav-link-to 'new-client'}}
				<h4>New Client</h4>
			{{/nav-link-to}}
			{{#nav-link-to 'new-patient'}}
				<h4>New Patient</h4>
			{{/nav-link-to}}
			{{#nav-link-to 'afterlogin'}}
				<h4>afterlogin</h4>
			{{/nav-link-to}}
			{{#nav-link-to 'login'}}
				<h4 href="#" {{action 'invalidateSession'}}>Logout</h4>
			{{/nav-link-to}}
		</li>
	</nav>
	{{/if}}
	<div class = "body">
		{{outlet}}
	</div>	
</div>


